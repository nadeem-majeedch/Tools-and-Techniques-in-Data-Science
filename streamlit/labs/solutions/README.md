# Streamlit Lab Solutions (Instructor Only)

Complete, verified solutions for Labs 1–4. Each file is a full `app.py`
that runs with `streamlit run`. Expected numbers are stated in comments
where they can be checked.

Verification notes:
- Lab 1, 2, 3, 4 solutions below were syntax-checked and the data/model
  logic was executed headlessly (see `../../scripts/verify_streamlit.py`).
- Random seeds are fixed (42) everywhere randomness appears.
- These are *reference* solutions; accept any student solution that meets
  the rubric in `../README.md`, not just these.